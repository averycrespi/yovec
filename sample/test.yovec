// Test program.

import n

let number N = -(1) + 2 - 3 * 4 / 5 % 6 ^ 7
let number M = abs sqrt sin cos tan arcsin arccos arctan 0

let vector V = [$n + 1]
let vector W = map abs (map +1 (map 1+ V))
let vector X = concat V V
let number V_A = V dot V + len V + (reduce + V)

export V as vector
