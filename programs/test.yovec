// Test program.

import n
import longname as o

let A = 1
let V = [0, 1, 2]
let M = [
    [0, 1],
    [2, 3]
]

let AA = map + (map - (map * (map / (map % (map ^ A A) A) A) A) A) A
let AB = map neg (map abs (map sqrt (map sin A)))
let AC = map < (map <= (map > (map >= (map == (map != A A) A) A) A) A) A
let AD = map ln 1

let VA = concat V V
let VB = dim V
let VC = dot V V
let VD = elem V 0
let VE = len V
let VF = reduce + V
let VG = repeat V 3
let VH = reverse V

let MA = matmul M M
let MB = transpose M

export A as num
export V as vec
export M as mat
